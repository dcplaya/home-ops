---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kometa
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: kometa
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        KOMETA_PLEX-API-KEY: "{{ .PLEX_API_KEY}}"
        KOMETA_TAUTULLI-API-KEY: "{{ .TAUTULLI_API_KEY }}"
        KOMETA_TMDB-API-KEY: "{{ .TMDB_API_KEY }}"
        KOMETA_RADARR-API-KEY: "{{ .RADARR__AUTH__APIKEY }}"
        KOMETA_SONARR-API-KEY: "{{ .SONARR__AUTH__APIKEY }}"
        KOMETA_TRAKT-ID: "{{ .TRAKT_ID }}"
        KOMETA_TRAKT-SECRET: "{{ .TRAKT_SECRET }}"
        KOMETA_TRAKT-ACCESS-TOKEN: "{{ .TRAKT_ACCESS_TOKEN }}"
        KOMETA_TRAKT-REFRESH-TOKEN: "{{ .TRAKT_REFRESH_TOKEN }}"
  dataFrom:
    - extract:
        key: kometa
    - extract:
        key: radarr
    - extract:
        key: sonarr
